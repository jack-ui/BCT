{% extends 'base.html.twig' %}

{% block content %}

<div class='banniere_form'>
    <h2 class="card-header info-color white-text text-center py-4">
            Profil de {{ app.user.nom }}
    </h2>
</div>
{# <div><img src="{{ asset('/photo/' ~ boutique.photo) }}" alt=" photo de la boutique"></img></div>  #}

<div class="container">
<div class="row">
    <div class="col-md-6 offset-3">
        <div class="card h-100">
            <div class="card-body text-center">
                <h4 class="card-title text-info">
                    Profil de {{ app.user.nom }}
                </h4>
                
                <p class="card-text"><strong>Prénom : </strong>{{ app.user.prenom }}<p>
                <p class="card-text"><strong>Nom : </strong>{{ app.user.nom }}<p>
                <p class="card-text"><strong>Email : </strong>{{ app.user.email }}<p>
                <p class="card-text"><strong>Adresse :</strong>{{ app.user.adresse }}<p>
                <p class="card-text"><strong>Ville : </strong>{{ app.user.ville }}<p>
                <p class="card-text"><strong>Code postal : </strong>{{ app.user.codePostal }}<p>
                <p class="card-text"><strong>Téléphone : </strong>{{ app.user.telephone }}<p>
                <p class="card-text"><strong>Date de naissance : </strong>{{ app.user.dateDeNaissance | date('d/m/Y') }}<p>
                <p class="card-text"><strong>Genre : </strong>{% if app.user.sexe == 'm' %}
                    <p>homme</p>
                    {% else %}
                    <p>femme</p>
                    {% endif %}
                <p>

            </div>
            <div class="row">
                <div class="col-md-12 text-center">
                    <a class="btn btn-outline-success" href="{{ path('admin_user_update', {id : app.user.id}) }}">Modifier </a>
                </div>
            </div>
        </div>
    </div>
</div>
</div>









{% endblock %}





