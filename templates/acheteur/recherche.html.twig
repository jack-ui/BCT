{% extends 'base.html.twig' %}


{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('/css/style_Form_Recherche.css') }}">
{% endblock %}

{% block content %}


    <div class='banniere_form'>
        <h2 class="card-header info-color white-text text-center py-4">
            Merci de renseigner un code postal ou un nom de produit
        </h2>
    </div>


    <div class="container">
        <br>
        <br>
        <!-- <div class="row ">
                            <form class="form-in-line col-lg-8 offset-md-4 text-center" method="GET" action="{{ path('search_results') }}">
                                <input class="form-control col-lg-8 text-center" type="search" placeholder="" aria-label="" name="champs">
                                <button class="btn btn-outline-success my-2 my-sm-2" type="submit">Search</button>
                            </form>
                        </div> -->
        <div class="input-group md-form form-sm form-2 pl-0">
            <form method="GET" class="input-group md-form form-sm form-2 pl-0" action="{{ path('search_results') }}">
                <input class="form-control my-0 py-1 lime-border" type="search" placeholder="Search" aria-label="Search">
                <div class="input-group-append">
                    <button class="btn btn-outline-success my-4 my-sm-2" type="submit">
                        <i class="fas fa-search text-grey"></i>
                    </button>
                </div>
            </form>
        </div>
        <br>
        <br>
        <br>
        {# compléter la fonction recherche pr le code postal #}
        <div class="row">
            <div class="col-md-12 text-center">
                <a class="btn btn-outline-success my-2 my-sm-2" href="{{ path('category', {'cat' : 'fruit' } )}}">FRUITS</a>
                <a class="btn btn-outline-success my-2 my-sm-2" href="{{ path('category', {'cat' : 'legume' } )}}">LEGUMES</a>
                <a class="btn btn-outline-success my-2 my-sm-2" href="{{ path('category', {'cat' : 'produit laitier' } )}}">PRODUITS LAITIERS</a>
                <a class="btn btn-outline-success my-2 my-sm-2" href="{{ path('category', {'cat' : 'oeuf' } )}}">OEUFS</a>
            </div>
        </div>
    </div>


    <br>
    <br>
    <br>
</div>{% endblock %}
